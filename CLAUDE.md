# CLAUDE.md

このリポジトリは「AIアプリ開発コース(バイブコーディング)」で生徒に見せる **完成形教材** を集めた場所です。
Claude Code でここを編集する際は、以下の前提に沿って動いてください。

## このリポジトリの位置づけ

- 生徒に「完成形」として配布・閲覧されるコードを置く場所
- 公開リポジトリ前提。**APIキー・個人情報・社内限定情報は絶対に含めない**
- **GitHub Pages で `docs/` フォルダを公開**している。配信ルートは `docs/`
- 各レッスンは `docs/<lesson-name>/` 配下にまとめる(基本は静的な HTML / JS / CSS)
- `docs/index.html` がレッスン一覧のトップページ


## ディレクトリ作成の規則

新しいレッスンを追加するときは:

- 配置先は `docs/<lesson-name>/`(GitHub Pages の公開対象に入れるため)
- ディレクトリ名は kebab-case(例: `snake-game`, `password-generator`)
- エントリポイントは `index.html`
- そのレッスン固有の補足があれば `docs/<lesson-name>/README.md` を作る
- 追加後は `docs/index.html` のレッスン一覧にリンクを追記する
- 不要なファイル(`.DS_Store`, `node_modules/`, ビルド成果物)はコミットしない(`.gitignore` 済み)

## やらないこと

- 機密情報・APIキー・トークンのコミット
- 生徒に伏せる必要のあるロジック(answer key 的なもの)の埋め込み — 教材は全公開前提
- 動かないコード・途中状態のコードのコミット(完成形リポジトリなので)
- 過剰なエラーハンドリングや抽象化 — 教材としての見通しを優先する
